#include <stdio.h>
struct student
{ 
    char name[50];
    int roll;
    float marks;
    float total;
    float average;
    char grade;
}
int main() {
    int n,i,j;
    printf("Enter the number of students: ");
    scanf("%d", &n)'
    struct student s[n];
    for(i=0; i<n; i++)
    printf("Enter name, roll number and marks of student %d: ", i+1);'
}